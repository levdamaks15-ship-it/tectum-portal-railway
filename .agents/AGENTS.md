# Tectum Portal Railway — Правила для агента

## 1. Общая архитектура

### Язык
- Все планирующие артефакты (`implementation_plan.md`, `walkthrough.md`) писать на русском языке.

### Рекомендации слэш-команд и режимов работы
- **Проактивное предложение режимов:** Агент обязан проактивно предлагать пользователю подходящую команду или режим работы в зависимости от этапа задачи:
  - **/owl** — при масштабных, архитектурно сложных задачах, проектировании новых модулей, глубоком рефакторинге или комплексной диагностике многоуровневых плавающих багов (мульти-агентный консилиум: исследователь, архитектор, рецензент).
  - **/grill-me** — на этапе сбора и уточнения требований перед проектированием/кодингом новой фичи, чтобы вскрыть скрытые допущения, краевые случаи и логические нестыковки через структурированное интервью.
  - **/goal** — для комплексных, сквозных или длительных задач «под ключ» (end-to-end реализация с автоматической верификацией и тестированием без преждевременных остановок).
  - **/review** — после написания/рефакторинга критических участков (эндпоинты FastAPI, миграции БД, сессии, валидации) для проверки безопасности, транзакционной целостности и соответствия стандартам проекта.
  - **/learn** — после успешного решения сложных нестандартных проблем, фикса хитрых багов или выработки новых соглашений, чтобы закрепить паттерн в правилах/базе знаний.
  - **/schedule** — при необходимости настройки отложенных проверок, таймеров или регулярного мониторинга задач.

### Git Push
- **Запрет автоматического пуша:** Никогда не выполнять и не предлагать `git push` без явного подтверждения от пользователя.

### Обязательное предварительное согласование плана (Feedback-Before-Code)
- **Строгий запрет преждевременного кодинга:** Перед выполнением любой задачи или поручения пользователя агент обязан сначала:
  1. Изложить пользователю своё понимание поставленной задачи.
  2. Описать конкретный план изменений (какие элементы интерфейса, функции, стили или эндпоинты будут затронуты).
  3. Запросить подтверждение пользователя.
- **Старт реализации:** Приступать к модификации файлов и написанию кода разрешено **только после явного одобрения** от пользователя, подтверждающего общее понимание задачи.

### База данных
- **Продакшн:** Используется исключительно облачная PostgreSQL на Railway (переменная окружения `DATABASE_URL`).
- **Локальная разработка:** Допускается SQLite (`tectum.db`) для тестирования.
- Миграции столбцов в `lifespan()` должны проверять тип драйвера (`postgresql` vs `sqlite`) и выполняться только для соответствующей БД.

### Почтовые уведомления и Email-шлюз (Email Notifications)
- **Запрет прямого SMTP на Railway:** Серверы Railway блокируют исходящие SMTP-порты (25, 465, 587). Использование `smtplib` для отправки писем в продакшене запрещено (вызывает `timed out`).
- **Основной шлюз отправки:** Отправка почтовых уведомлений планнера задач осуществляется исключительно через **Google Apps Script Webhook** (HTTPS POST на порт 443, переменная `GMAIL_WEBHOOK_URL`). Это обеспечивает отправку с личного/корпоративного Gmail на любые почтовые адреса без необходимости привязки внешних доменов.
- **Безопасность кодировки эмодзи в письмах:**
  - В теле HTML-шаблона письма все эмодзи и спецсимволы обязаны конвертироваться в стандартные **HTML-сущности** (`_convert_emojis_to_entities`: `&#128197;`, `&#128100;` и т.д.), чтобы исключить искажение символов в `??????` в почтовых клиентах.
  - В заголовке темы письма (`subject`) использование эмодзи запрещено (применять `_clean_subject`), тема должна оформляться чистым текстом вида `[Tectum] Название события`.

## 2. Развёртывание (Railway)

### Mise/Python
- В корне репозитория обязательно должен быть `mise.toml`:
  ```toml
  [settings]
  python.github_attestations = false
  ```

### Procfile
```procfile
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```

## 3. Номенклатура продуктов

- **«Шифер 8 волн рифленый»** — строго утверждённое название. Всегда использовать именно его.
- **Автотрансляция:** При импорте из внешних источников устаревшее «Шифер 8 волн пиленый» должно автоматически заменяться на «Шифер 8 волн рифленый».

## 4. Данные и роли

### Полная свобода данных (единый кабинет)
- Все данные абсолютно общие. Любой мастер смены (роль `master`) имеет право просматривать любые графики, сводки, выработку и расходы сырья других мастеров, а также свободно вносить и редактировать любые рапорты смен.
- Администраторы (`admin`), технологи (`technologist`), директора (`director`) имеют полный доступ ко всем данным.
- **Правила изоляции мастеров НЕ применяются.** Исторические данные не фильтруются по `master_id`.

### Единый рапорт мастера
- Авторизация: мастер заходит под единой ролью «Мастер смены» (PIN или SSO), затем выбирает конкретное имя из выпадающего списка.
- **Отсутствие концепции открытия/закрытия смен для пользователей.** Жизненный цикл active/closed скрыт. Баннер текущей смены не отображается. Мастер заполняет независимые сменные рапорты за выбранную дату/смену/линию.
- **Единая форма ввода:** Производство (листы, сбросы, замесы), переборка (на склад ГП, 1 сорт), брак Дестакера (11 полей), расход/приход сырья — всё в одной форме.
- Исключение брака СКК/ОТК: поле брака СКК скрыто с формы мастера (учитывается в отдельной системе).
- Масса выработки рассчитывается на клиенте в кг и тоннах с точным совпадением названий продуктов.
- Терминология: «Разборщик» → «Дестакер».

### Навигация и нативные ссылки (UX внешних разделов)
- **Карточки внешних разделов на главном экране:** Карточки, ведущие на отдельные страницы («База знаний», «Кабинет чек-листов» и т.д.), обязаны рендериться нативным тегом `<a href="...">`, а не `<div onclick="...">`. Это гарантирует возможность открытия раздела в новой вкладке кликом колёсика мыши (средней кнопкой) или сочетанием `Ctrl+клик`.
- **Внешние модули в панели вкладок кабинета мастера:** Навигационные кнопки/ссылки на внешние сервисы («Кабинет чек-листов», «База знаний») на панели вкладок кабинета мастера (`#tabs-menu`) обязаны содержать `target="_blank"`, чтобы их открытие не сбрасывало рабочую сессию мастера и не прерывало заполнение сменного рапорта.

### Поддержка нескольких продуктов и партий в рамках одной смены (Переход продукции)
- **Уникальность записи рапорта:** Производственный рапорт (запись в таблице `shifts`) уникально идентифицируется набором параметров: `(date, shift_name, line, product_name, batch_number)`.
- **Запрет перезаписи (Backend):** При поиске существующей смены в БД (в эндпоинтах `POST /api/report` и `GET /api/shifts/by_params`) обязательно включать в фильтр поля `product_name` и `batch_number`. Если в течение смены произошёл переход на другую продукцию или сменился номер партии, бэкенд обязан создавать новую независимую запись `Shift`, а не перезаписывать предыдущий рапорт этой смены.
- **Реакция интерфейса (Frontend):** При изменении пользователем полей продукции (`#rep-product`) или номера партии (`#rep-batch`) фронтенд обязан инициировать запрос к API (`onProductChange` с передачей параметров `product_name` и `batch_number` в URL query string). При ответе `404 Not Found` форма должна очищать числовые показатели выработки для ввода новой партии, сохраняя при этом выбранные параметры смены (дату, смену, линию, мастера, продукт и номер партии).
- **Хронологическая сортировка партий (Backend):** При формировании списков смен, отчётов, графиков и экспортов (где осуществляется выборка `models.Shift` с сортировкой по дате), в блок `.order_by(...)` обязательно включать сортировку по номеру партии (`batch_number`) перед сортировкой по `id`. Для убывающей сортировки по дате использовать `models.Shift.batch_number.desc(), models.Shift.id.desc()`, а для возрастающей — `models.Shift.batch_number.asc(), models.Shift.id.asc()`. Это гарантирует правильный порядок отображения партий при переходе продукции в рамках одной смены, независимо от порядка или времени создания записей в БД.
- **Агрегация тонн при нескольких рапортах за смену (Backend Aggregation):** При формировании сводок, отчётов и экспортов (суточных, недельных, месячных в JSON или Excel), если в рамках одного слота смены `(date, shift_name, line)` существует несколько записей рапортов (при переходе продукции или смене номера партии), **категорически запрещено** рассчитывать общий вес слота путем умножения общего числа листов на средний вес одной (например, последней) партии или перезаписывать значение `tons` в цикле. Код обязан честно **аккумулировать (суммировать)** фактические тонны всех рапортов в слоте: `sum(r.lfm_sheets * get_product_finished_weight_kg(db, r.product_name) / 1000.0)`. Для расчета плановых тонн (`plan_tons`) или средних показателей слота следует вычислять истинный средневзвешенный вес только после суммирования всех фактических тонн и листов слота (`avg_w = (total_tons * 1000.0) / total_sheets`).


### CRUD и администрирование

**Разрешено редактирование для сущностей:**
- `models.Master` — сотрудники
- `models.ProductNorm` — нормативы расхода
- `models.Downtime` — простои
- `models.DowntimeDirectory` — справочник простоев
- `models.MonthlyPlanBoard` — план-факт доска

**План-факт доска (`MonthlyPlanBoard`):**
- Обновляется через Upsert (`POST /api/plan_board`).
- Все изменения (CREATE, UPDATE, DELETE) логируются в `AuditLog` с подробным описанием старых и новых значений.

**Администраторы (`admin`):**
- Имеют права на полное редактирование (UPDATE) и удаление (DELETE) смен, отчётов ЛФМ, партий и простоев (даже если смена закрыта в БД).
- Все изменения логируются в `AuditLog` со сравнением старых и новых значений.

**Безопасность DOM на фронтенде:**
- При изменении стилей/классов/атрибутов всегда проверять существование элемента: `const el = document.getElementById(id); if (el) { ... }`
- Категорически запрещено вызывать `.style` или `.classList` напрямую от `document.getElementById`.

### Стандарты интерфейса и дизайн-системы (Light Theme & Contrast UI)
- **Единая светлая палитра:** Весь интерфейс портала (главная, кабинет мастера, админ-панель, база знаний, планнер, аналитика) оформляется строго в единой чистой светлой теме.
  - Фоны страниц: `--bg-color: #f0f2f5`, `--body-bg-gradient`.
  - Карточки, модальные окна, сайдбары и мобильные шапки: `#ffffff` с разделителями `#e2e8f0` / `#cbd5e1`.
  - Запрещено использовать тёмные/чёрные фоны сайдбаров (`rgba(10, 15, 30, ...)`) или инпутов внутри светлых панелей.
- **Вторичные кнопки и кнопки «Отмена»:**
  - Все нейтральные кнопки (кнопки закрытия, отмены, возврата на портал, фильтров) обязаны иметь класс `.btn-secondary` или `.btn-cancel` с явным контрастным цветом текста (`#334155` / `#0f172a`), светлым фоном `#f8fafc` и рамкой `#cbd5e1`.
  - Категорически запрещено оставлять `background: transparent` или `background: rgba(255,255,255,0.1)` без переопределения цвета текста, чтобы исключить появление белого текста на белом фоне.
- **Контрастность бейджей в таблицах:**
  - Текстовые бейджи смен, статусов и категорий должны использовать пастельные контрастные цвета для светлого фона:
    - Смена «☀️ День»: фон `#fef3c7`, текст `#b45309`, рамка `#fde68a`.
    - Смена «🌙 Ночь»: фон `#e0f2fe`, текст `#0369a1`, рамка `#bae6fd`.
    - Успех / 1 сорт: фон `#ecfdf5`, текст `#047857`, рамка `#a7f3d0`.
    - Нейтральные / Экспорт: фон `#f1f5f9`, текст `#475569`, рамка `#e2e8f0`.
- **Мобильная адаптивность:**
  - Мобильные шапки и выезжающие шторки меню обязаны иметь светлый фон `#ffffff`, контрастные кнопки (`.mobile-menu-btn` с тёмной иконкой) и мягкое затемнение оверлея (`rgba(15, 23, 42, 0.4)`).
- **Горизонтальные панели фильтров (Inline Flex Toolbar):**
  - Панели фильтрации и контролов над отчетами, сводками и журналами обязаны оформляться в **единую компактную строку (`display: flex; flex-wrap: wrap; gap: 1rem; align-items: flex-end;`)** с высотой элементов `38px` и `flex: 1` для равномерного заполнения ширины, исключая вертикальные «лесенки».
- **Стандарт печати и выгрузки в PDF (Native Print / Microsoft Print to PDF):**
  - Во всех модулях портала (планнер, сводка выработки, журнал, аналитика) кнопки экспорта в PDF и печати обязаны использовать **нативное диалоговое окно печати (`window.print()`)**. Это обеспечивает возможность как прямой отправки на принтер, так и чистого векторного сохранения в PDF («Microsoft Print to PDF» / «Сохранить как PDF»).
- **Синхронизация динамических заголовков периодов:**
  - При переключении интервалов отчетов («Весь месяц» ↔ «Конкретная неделя») фронтенд обязан синхронно обновлять заголовок блока на странице и в печатном бланке («Месячная сводка выработки» ↔ «Недельная сводка выработки»).

## 5. Защита целостности базы данных (Database Integrity)

**Общий принцип:** Любое изменение кода не должно нарушать целостность данных в БД. При редактировании кода всегда учитывать существующую структуру данных.

### 5.1. Проверка внешних ключей (Foreign Keys)
- **Перед созданием/обновлением записи**, которая ссылается на другую таблицу (например, `shift.master_id` → `masters.id`, `monthly_plan_board.master_id` → `masters.id`), **всегда проверять**, что целевая запись существует.
- Если запись не найдена — возвращать ошибку 400/404, а не создавать битую ссылку.
- **Запрещено** записывать `master_id`, `shift_id` или любой другой внешний ключ без валидации его существования в родительской таблице.

### 5.2. Транзакционная безопасность
- Групповые операции (создание смены + отчёта ЛФМ + партии) должны выполняться в одной транзакции.
- При ошибке в любой части — полный `rollback()`, чтобы не осталось «полу-созданных» записей.
- Все `db.commit()` должны предваряться проверками, а при ошибке — `db.rollback()`.

### 5.3. Защита эндпоинтов от 500-ошибок
- Все `GET` эндпоинты, возвращающие списки (`/api/shifts/all`, `/api/plan_board`, `/api/report/summary` и т.д.), **обязательно** должны иметь try/except блок.
- При сериализации связанных моделей (`.master.name`, `.master.id`) **всегда** проверять, что связанный объект не `None`.
- Если связанная запись отсутствует — выводить плейсхолдер («Н/Д», `null`, `""` или `0`), а не падать с `AttributeError`.

### 5.4. Защита от удаления связанных данных
- **Запрещено удалять мастера**, если на него ссылаются смены (`shifts`) или план-факт доска (`monthly_plan_board`). Код должен проверять наличие связанных записей перед удалением (уже реализовано в `delete_master`).
- При удалении смены — каскадно удалять связанные отчёты ЛФМ, партии и простои (уже реализовано в `admin_delete_shift`).

### 5.5. Graceful degradation (отказоустойчивость)
- Если данные в БД повреждены (например, битый `master_id`, неверный формат даты), эндпоинт должен **продолжить работу** и вернуть частичные данные, а не падать с 500.
- Использовать конструкцию:
  ```python
  master_name = s.master.name if s.master else "Мастер удалён"
  ```
  для всех связанных полей.

### 5.6. Миграции без потери данных
- Добавление новых колонок — только через `ALTER TABLE ADD COLUMN` с `try/except` (уже реализовано в `lifespan()`).
- Переименование/удаление колонок — только если точно известно, что на них нет зависимостей в коде.
- **Запрещено** менять тип существующей колонки, если в ней уже есть данные (может вызвать потерю данных).

### 5.7. Бэкапы перед крупными изменениями
- Перед выполнением массовых операций (очистка данных, импорт из Excel, изменение схемы БД) агент должен предложить пользователю сделать бэкап базы данных.
- Если это продакшн-БД (PostgreSQL на Railway) — предложить сделать дамп через `pg_dump` или через встроенные бэкапы Railway.

### 5.8. Миграции исторических данных (Historical Data Cleanups)
- Изменение логики синхронизации или обработки данных в API (например, устранение багов, приводящих к появлению "фантомных" записей) **не исправляет** уже поврежденные исторические данные в базе.
- При исправлении багов синхронизации всегда проверяйте необходимость исправления старых данных. **Для продакшена на Railway (где ручной запуск скриптов затруднен)** предпочтительным способом является добавление одноразовой логики исправления напрямую в функцию **`lifespan()`** (FastAPI startup event). Это гарантирует автоматическое применение Data-миграции сразу после деплоя. Административные эндпоинты (как `/api/admin/...`) следует использовать только для тех исправлений, которые требуют ручного контроля или ввода параметров пользователем. Не оставляйте "мусор" в БД после исправления бэкенд-логики.

### 5.9. Нечеткий поиск (Fuzzy Matching)
- Названия производственных линий могут храниться в БД и приходить с фронтенда в разных форматах (например: `ЛФМ-1`, `Линия 1`, `Линия 1 (ЛФМ-1)`).
- При написании запросов в SQLAlchemy избегайте строгого равенства `==` для текстовых идентификаторов линий. Используйте подстрочный поиск (`models.Shift.line.like("%1%")` или `"1" in value`), чтобы избежать потерь данных при синхронизации и объединении отчетов.

## 6. Производственный план (План-факт)

### Фиксированные нормы (не расчёт от месячного плана)
- Обычный день (план): **2700 листов**
- Ночь (план): **3300 листов**
- Санитарный день (понедельник, смена «День»): **0 листов**

### Глобальные KPI и Потолок Плана (Dashboard)
- **Жесткий потолок:** Для глобальных метрик (на портале) установлен жесткий бизнес-потолок: **160 000 листов в месяц** и **39 000 листов в неделю**. Эти цифры являются безусловной истиной.
- **Пропорциональный расчет "Плана на сегодня":** Показатель "План на сегодня" (для виджетов отставания) высчитывается строго пропорционально количеству календарных дней в выбранном периоде. Формула: `(Потолок_плана / Календарных_дней_в_периоде) * Прошло_календарных_дней`. Запрещено суммировать для этого нормативы отработанных смен.
- **Динамический расчет планового тоннажа:** План в тоннах всегда пересчитывается динамически на основе **фактического среднего веса** выпущенной продукции за этот период (`фактические_тонны * 1000 / фактические_листы`). Жесткая константа (19.6 кг) используется только как фоллбэк при полном отсутствии фактических данных. Это необходимо для честного расчета отставания в тоннах (например, при выпуске шифера 7 волн со средним весом ~17.07 кг).

### Конвертация смен в дни (Shift to Days Calculation)
- **Удвоение массива смен:** На бэкенде в API-эндпоинтах (`get_daily_report` и др.) сетка ожидаемых смен всегда формируется из расчета строго 2 смены («День» и «Ночь») на каждый календарный день периода.
- **Подсчет дней на фронтенде:** При вычислении количества **календарных дней** в периоде (неважно, полная это неделя на 7 дней, или неполная на 2, 3, 5 дней), категорически запрещено использовать прямую длину массива смен от бэкенда (`data.days.length`) как количество дней. 
- Длину массива необходимо **всегда делить на 2**, так как он содержит смены, а не дни (`totalDays = data.days.length / 2`). 
  - *Пример: Если неделя состоит из 5 дней, бэкенд вернет массив из 10 элементов (5 дней × 2 смены). Деление 10 / 2 даст корректные 5 календарных дней.*
- Ошибка в этом расчете приведет к занижению пропорционального дневного плана и, как следствие, фиктивному «перевыполнению» в виджетах.

### Факт (fact_sheets)
- Формируется на основе `LFMReport.lfm_sheets` (общее количество сформованных листов от машиниста ЛФМ).
- Любые изменения отчётов ЛФМ (создание, изменение, удаление) автоматически обновляют `fact_sheets` в `MonthlyPlanBoard` через `sync_lfm_to_plan_board()`.

### Sanitary Day (понедельник, смена «День»)
- План = 0.
- Если фактическое производство есть — оно полностью записывается (перевыполнение плана).

### Сетка отчётов (Reporting Architecture)
- План статичен. Отчёты (графики, недельные таблицы) всегда генерируют полную сетку ожидаемых смен (например, 14 смен за неделю) со статическими плановыми числами.
- Фактические данные накладываются поверх этой сетки.

## 7. Расчёт производительности и потерь

### Цикл формовки
- Один цикл = **26 секунд**.
- За цикл выпускается **2 листа**.
- Исключение: для «Шифер 7 волн 3500*980» — **1 лист** за цикл.

### Потери при простое
- Расчёт на бэкенде динамически:

  $$\text{Потери (тонн)} = \frac{\text{Длительность (сек)}}{26} \times \text{Выход листов за цикл} \times \frac{\text{Вес листа из ProductNorm (кг)}}{1000}$$

## 8. Журнал простоев и AI-классификатор (Gemini Flash)

- **На портале** — форма ввода новых простоев и оперативный просмотр за выбранную смену. Аналитический экспорт — в Google Sheets (лист «Простои»).
- **Ленивое создание смен (Lazy Creation):** При просмотре дат/смен бэкенд НЕ создаёт пустые смены (возвращает 404). Смена создаётся на лету только при отправке записи простоя (`create_if_not_exists=true`).
- **Регистрация без медиафайлов:** При регистрации простоев используется текстовая аналитика. Загрузка фото/видео и запись голоса исключены из формы ввода.
- **Автоэкспорт в Google Sheets:** При создании, изменении или удалении простоя автоматически запускается фоновая задача `sync_downtimes_bg()` → `export_downtimes_to_google_sheets()`.

### 8.1. AI-классификатор (Gemini Flash Cascade)
- **Динамический справочник:** Классификатор `downtime_classifier.py` считывает актуальные участки и узлы из таблицы `models.DowntimeDirectory` в БД (с 5-минутным кэшированием в памяти), автоматически учитывая изменения из Google Таблицы без правок в коде.
- **Приоритет моделей:** Запросы отправляются по каскаду: `gemini-3.5-flash-lite` -> `gemini-flash-lite-latest` -> `gemini-3.5-flash` -> `gemini-3.7-flash` -> `gemini-flash-latest` -> локальный Regex/Fuzzy классификатор.
- **Цеховая привязка узлов:**
  - «Ковшевая мешалка» и «Гомогенизатор» относятся строго к участку **«ЛФМ»**.
  - Упоминание работы «на ходу» / «без останова» автоматически снимает флаг останова оборудования (`is_equipment_downtime = False`).
- **Прозрачное логирование:** Каждый вызов AI обязан выводить в консоль сервера строку формата: `[AI Gemini Flash] '<текст>' -> Модель: <модель> (<время>s) -> [<участок>] / [<узел>] / [<категория>]`.

### 8.2. Авторство смены в журнале простоев
- Авторство смены (`shift.master_id`) принадлежит мастеру, создавшему рапорт смены или первому зафиксировавшему простой до создания рапорта.
- В «Журнале простоев» при выборе даты и смены интерфейс автоматически синхронизирует выпадающий список мастера с `shift.master_id`, обеспечивая точность выгрузки в Google Таблицу.

## 9. Google Sheets интеграция

Вся производственная аналитика дублируется в единый Google Документ (`GOOGLE_SPREADSHEET_ID`).

### Подключение
- Аутентификация: сервисный аккаунт Google через `google_credentials.json` (локально) или переменную окружения `GOOGLE_CREDENTIALS_JSON` (Railway/Render).
- Функция `get_sheets_service()` сначала пробует `GOOGLE_CREDENTIALS_JSON`, затем локальный файл.

### Листы Google Таблицы

| Лист | Функция экспорта | Содержание |
|------|-----------------|------------|
| **Сводный отчет** | `sync_report_to_google_sheets()` | Полная сводка по всем сменам: дата, партия, линия, смена, мастер, продукт, формовка, кондиция, брак, расход сырья, отклонения в %. Вызывается при фоновой синхронизации после сохранения рапорта. |
| **Приход сырья** | `export_receipt_to_google_sheets()` | Данные прихода сырья по сменам: дата, смена, линия, мастер, все виды сырья (хризотил, цемент по силосам, целлюлоза и т.д.). Вызывается при сохранении рапорта и при добавлении/удалении записи прихода. |
| **Нормативы** | `export_norms_to_google_sheets()` | Таблица нормативов расхода сырья по продуктам. Двусторонняя синхронизация: также есть `sync_norms_from_google_sheets()` для обратного импорта. |
| **Справочник простоев** | `export_downtime_directory_to_google_sheets()` | Каталог типовых поломок по участкам/узлам. Двусторонняя синхронизация: также есть `sync_downtime_directory_from_google_sheets()`. |
| **Простои** | `export_downtimes_to_google_sheets()` | Журнал простоев (вызов из main.py через `sync_downtimes_bg`). |

### Правила синхронизации
- **При создании/изменении/удалении простоя** → фоновая синхронизация простоев в Google Sheets.
- **При сохранении/обновлении рапорта смены** → фоновая синхронизация сводного отчёта и прихода сырья.
- **При добавлении/удалении записи прихода сырья** → фоновая синхронизация прихода.
- Синхронизация нормативов и справочника простоев — по запросу из админ-панели (кнопки синхронизации).
- Если `GOOGLE_SPREADSHEET_ID` не задан или равен `1_mock_...` — синхронизация пропускается с логом в консоль.

### 9.1. Форматирование сводного отчёта и целостность таблиц
- **Динамический автофильтр:** При каждой синхронизации (`sync_report_to_google_sheets`) диапазон автофильтра (`setBasicFilter`) обязан безусловно пересчитываться на полный актуальный размер `total_rows`, предотвращая выпадение новых строк из диапазона таблицы.
- **Форматирование дат:** Колонка дат (Колонка A) обязана принудительно форматироваться правилом `numberFormat: {type: "DATE", pattern: "dd.mm.yyyy"}` с горизонтальным выравниванием по центру (`CENTER`).
- **Единый шрифт:** Ко всем строкам и колонкам таблицы обязательно применяется единый шрифт и размер (`Arial`, `9pt`), гарантируя единообразие оформления для всех новых записей.

## 10. Автоматический экспорт в SharePoint

- При сохранении/обновлении рапорта смены (`POST/PUT /api/report`) запускается фоновая генерация Excel-отчёта и загрузка в SharePoint (папка `Reports`).
- **Обработка блокировки (HTTP 423):** Если файл заблокирован пользователем — сохранить локальную копию, залогировать предупреждение, поставить фоновую задачу повторной загрузки (`upload_sharepoint_report_retry`).

### Excel Tables
- Экспортируемые Excel-данные должны быть оформлены как форматированные таблицы (ListObject) с полосатой разметкой и фильтрацией.

## 11. Безопасный импорт данных (Safe Parsing)

- При парсинге Excel-файлов все числовые поля приводить через защитные функции-конвертеры (`safe_int`, `safe_float`) для предотвращения `ValueError` от пробелов, пустых строк, символов форматирования.

## 12. Microsoft 365 интеграция

- Всегда проактивно анализировать возможность интеграции с M365 (SharePoint, Excel Online, Teams, Outlook, MS Graph API).
- Использовать существующую авторизацию MSAL и Graph API.
- Сохранять отказоустойчивость: локальный кэш на случай сбоев сети.
- При разработке интеграций, требующих токенов/ключей, обязательно напоминать пользователю добавить переменные окружения в панель Railway/Render.

## 13. Кэширование (Cache-Busting)

- Все GET-запросы к `/api/*` должны содержать уникальный параметр `?_ts=...` для борьбы с агрессивным мобильным кэшированием.
- При изменении `style.css`, `app.js`, `admin.js` — инкрементировать версию `?v=` в `index.html`/`admin.html`.

## 14. Фильтрация пустых смен (Phantom Shifts) в отчётах

- Из-за логики "Ленивого создания смен" (Lazy Creation) при регистрации простоев могут создаваться пустые смены ("фантомные"), не имеющие выработки.
- При формировании любых сводных таблиц (`get_report_summary`), дашбордов (`get_daily_report`, счетчик `total_shifts`), выгрузке в Google Sheets и получении списков смен (`/api/shifts/all`) **необходимо строго исключать** абсолютно пустые смены, у которых одновременно:
  - `plan_sheets = 0`
  - `lfm_sheets = 0`
  - `warehouse_gp = 0`
  - `zo_batches = 0`
  - не отправлен отчёт по ЗО (`zo_submitted = False`)
- **Важно:** Наличие записей о простоях (`downtimes`) или приходах сырья (`receipts`) **НЕ делает** смену "рабочей". Если нет выработки и плана (условия выше), смена считается пустой и должна исключаться из подсчета количества смен и статистики производительности.

## 15. Автовыбор смены в журналах

- При загрузке журналов (например, простоев) автоматически выбирать последнюю доступную смену и сразу запрашивать данные.

## 16. Принцип отладки

- **Last Known Good State:** При возникновении ошибки сначала найти и проанализировать последнее рабочее состояние (коммиты, бэкапы БД, исходный код). Установить причину, затем вносить правки. Не менять «наугад».

## 17. Переменные окружения

При разработке или изменении интеграций с внешними API (Google Sheets, SharePoint, Telegram Bot и др.), требующих токенов/ключей/идентификаторов (`GOOGLE_SPREADSHEET_ID`, `GOOGLE_CREDENTIALS_JSON`, `M365_CLIENT_ID`, `M365_TENANT_ID`, `M365_CLIENT_SECRET` и т.п.), агент ОБЯЗАН напомнить пользователю добавить эти переменные в панель управления хостинга (Railway/Render). Не предполагать, что локальный `.env` автоматически синхронизируется с облаком.

## 18. Хронологическая сортировка смен (Shift Sorting Order)

При написании или изменении любых SQLAlchemy-запросов к `models.Shift` (для списков смен, сводных таблиц, журналов, выгрузок в Excel, Google Sheets или SharePoint) **категорически запрещено** использовать ID записи (`models.Shift.id`) сразу после даты в `.order_by()`. Поскольку смены могут вноситься в БД не по порядку или задним числом, сортировка по `id` приводит к хаотичным скачкам типов смен («День»/«Ночь») и номеров партий.

**Обязательные правила сортировки смен:**
1. **Для списков и логов от новых к старым (`date.desc()`):**
   ```python
   .order_by(models.Shift.date.desc(), models.Shift.line.asc(), models.Shift.shift_name.desc(), models.Shift.id.desc())
   ```
   *Почему `shift_name.desc()`:* В русском алфавите «**Н**очь» находится позже, чем «**Д**ень». При сортировке по убыванию «Ночь» выводится первой (так как в рамках суток ночная смена идет позже дневной), а «День» — второй. Это гарантирует правильный хронологический порядок событий и убывание номеров партий сверху вниз.

2. **Для хронологических отчётов от старых к новым (`date.asc()`):**
   ```python
   .order_by(models.Shift.date.asc(), models.Shift.line.asc(), models.Shift.shift_name.asc(), models.Shift.id.asc())
   ```
   Здесь `shift_name.asc()` ставит утреннюю смену («День») перед вечерней («Ночь»).

3. **Сортировка связанных коллекций в моделях (`relationship`):**
   В `models.py` при объявлении связей (например, `batches`, `lfm_reports`, `downtimes`, `receipts`) всегда явно указывать параметр `order_by` (например, `order_by="Batch.id"`, `order_by="Downtime.start_time"`), чтобы связанные данные всегда загружались предсказуемо и упорядоченно.

## 19. Сохранение состояния интерфейса (Frontend State Preservation)

Для улучшения пользовательского опыта (UX) при работе с веб-интерфейсами (SPA-подобные переходы, большие формы) необходимо применять следующие паттерны сохранения состояния:

- **Автосохранение черновиков форм:** Использовать `localStorage`. Данные форм (особенно больших рапортов) должны сохраняться локально при вводе и загружаться при загрузке страницы. При успешной отправке данных на сервер `localStorage` должен очищаться, чтобы следующий ввод начинался с чистого листа.
- **Состояние UI и вкладок:** Использовать `sessionStorage`. Активная вкладка (например, "Журнал простоев", "Месячная сводка") или фильтр должны сохраняться в `sessionStorage`. При случайном обновлении страницы (F5) пользователь должен оставаться на том же месте. При открытии приложения в новой вкладке сессия будет чистой.

## 20. Оптимизация запросов БД (Избегание N+1)

Во избежание падений сервера по таймауту (502 Bad Gateway) из-за проблемы N+1 запросов:
- **Жадная загрузка (Eager Loading):** При массовой выборке смен (`models.Shift`) для списков, отчетов или экспортов **всегда** используйте жадную загрузку для связанных коллекций (`lfm_reports`, `receipts`, `downtimes`), по которым планируется итерация. Пример: `db.query(models.Shift).options(selectinload(models.Shift.lfm_reports), selectinload(models.Shift.receipts), selectinload(models.Shift.downtimes))`
- **Кэширование справочников:** Если в цикле по отчетам требуется обращение к статичным справочникам (например, `ProductNorm` для получения веса), используйте in-memory кэширование (например, глобальный `dict` с небольшим TTL на уровне модуля), чтобы не делать `db.query()` на каждой итерации.

## 21. UX / Пользовательский интерфейс

### Поля ввода чисел (Number Inputs)
- **Глобальная очистка нулей:** Во всех полях `<input type="number">` с дефолтным значением `0` работает глобальный скрипт (в `app.js`). На событие `focusin` значение `0` стирается (для предотвращения случайного ввода вида `50` вместо `5`). На событие `focusout` пустое значение заменяется обратно на `0` с вызовом события `input`.
- **Запрет на дублирование:** При создании новых полей ввода чисел на фронтенде не пишите локальные обработчики `focus` для очистки нулей, так как это уже обрабатывается глобально. Просто используйте `<input type="number">`.

## 22. Развертывание OnlyOffice (Railway)

При интеграции или переразвертывании `onlyoffice/documentserver` на Railway необходимо строго соблюдать следующие правила, чтобы избежать критических ошибок запуска:

1. **Запрет использования тега `:latest`**: Официальный образ `onlyoffice/documentserver:latest` может содержать баги скриптов инициализации (например, `basename: missing operand`). Всегда жестко фиксируйте версию образа в настройках Railway (например, `onlyoffice/documentserver:8.0`).
2. **Баг с `COMPANY_NAME` и томами (Volumes)**: Если при запуске супервизор падает с ошибкой `The directory named as part of the path /var/log/COMPANY_NAME/... does not exist`, это означает, что скрипт инициализации не заменил переменную в конфигах. Это часто происходит при редеплое с существующим Volume. **Решение**: Полностью удалить привязанный Volume и позволить Railway создать его с чистого листа, либо вручную задать переменную окружения `COMPANY_NAME=onlyoffice`.
3. **Падение Nginx (IPv6 duplicate server)**: Если в логах есть `* Starting nginx nginx ...fail!`, а команда `nginx -t` выдает ошибку `[emerg] a duplicate default server for [::]:80 in /etc/nginx/sites-enabled/default`. **Решение**: Конфликт дефолтного конфига Ubuntu. В консоли контейнера необходимо выполнить: `rm /etc/nginx/sites-enabled/default && nginx -t && service nginx start`.
4. **JWT Токены**: В новых версиях OnlyOffice по умолчанию включена проверка токенов (`JWT_ENABLED=true`), что вызывает ошибку в интерфейсе «Токен безопасности документа имеет неправильный формат». Если бекенд не подписывает токены, в переменных окружения Railway необходимо установить `JWT_ENABLED=false` и удалить `JWT_SECRET`.

## 23. Система электронных чек-листов и структура участков завода

Модуль чек-листов (`/static/checklists.html`) предназначен для оперативного контроля состояния оборудования, 5S и передачи смен между сотрудниками без бумажных журналов с мгновенной фиксацией замечаний.

### 23.1. Утвержденная номенклатура производственных участков

На заводе действует строгая классификация производственных участков и привязка должностей:

1. **Цех ХЦИ (Цех хризотилцементных изделий)**:
   - **Строгое правило:** Для Мастеров смены подразделением является исключительно **«Цех ХЦИ»** (не «ЛФМ» и не «Общезаводской»). Мастер руководит всем цехом.
2. **ЛФМ (Листоформовочная машина)**:
   - Должности: `Машинист`, `Помощник машиниста`.
3. **Дестакер (Разборщик стоп)**:
   - Должности: `Оператор дестакера`.
4. **Стакер (Укладчик сырых листов)**:
   - Должности: `Оператор стакера`.
5. **Заготовительное отделение (ЗО)**:
   - Должности: `Оператор ЗО`.
6. **Рекуператоры и целлюлоза**:
   - **Строгое правило:** Выделен в самостоятельный участок (не смешивать с ЗО).
   - Должности: `Оператор целлюлозы и рекуператора`.
7. **Участок дозировки**:
   - Должности: `Дозировщик`.
8. **Сменные ремслужбы**:
   - **Строгое правило:** Выделены в самостоятельный сменный участок дежурной бригады.
   - Должности: `Дежурный слесарь`, `Дежурный электрик`.
9. **СКК (Служба контроля качества)**:
   - Должности: `Лаборант`.
10. **Слесарная мастерская**:
    - Стационарный участок для дневного ремонтного персонала.
11. **Зона ремонта насосов**:
    - Профильный участок ремонта насосного парка цеха.
12. **Участок ремонта цилиндров**:
    - Участок подготовки и перетяжки сетчатых цилиндров.
13. **Котельная**:
    - Энергетический участок цеха (должность: `Оператор котельной`).

### 23.2. Логика передачи смены и сопоставления дежурных бригад

- **Парность смен (Прием ➔ Сдача):** 
  Прием-передача оборудования всегда происходит между двумя конкретными сменами:
  - Для смены **«День» (08:00 – 19:00):** принимающая смена — текущий день, а сдающая — смена **прошлой ночи**.
  - Для смены **«Ночь» (19:00 – 08:00):** принимающая смена — текущая ночь, а сдающая — смена **сегодняшнего дня**.
  - В интерфейсе и бейдже дежурства всегда выводится парное обозначение: `Смена X (прием) ➔ Смена Y (сдача)`.
- **Автоматическая фильтрация участников:**
  - В чек-листе мастера в списках «Принимающий» и «Сдающий» выводятся строго мастера соответствующих смен.
  - В чек-листе рабочих при выборе участка (например, «Сменные ремслужбы» или «Дестакер») выпадающие списки моментально фильтруют рабочих именно этого участка из принимающей и сдающей смен.
- **Одноэкранная форма передачи:**
  Приемка и сдача фиксируются в единой быстрой экранной форме (15–20 секунд на заполнение) с указанием замечаний и чек-боксами по пунктам проверки.

### 23.3. Архитектура данных, экспорт и фоновые задачи

- **Автономность портала и базы данных (PostgreSQL):**
  Сотрудники (`models.ChecklistEmployee`), график сменности (`models.ShiftScheduleEntry`) и история заполненных чек-листов (`models.ChecklistSubmission`) хранятся в PostgreSQL на Railway.
- **Управление через Админ-панель (`/admin`):**
  - **Вкладка «Сотрудники цеха (Чек-листы)»:** просмотр списка, добавление, редактирование ФИО, смены, участка, должности и удаление сотрудников.
  - **Вкладка «График сменности»:** просмотр календаря смен и возможность переназначить дежурные бригады на любой день.
- **Выделенная Google Таблица чек-листов и премирования:**
  - Целевая таблица задаётся через `CHECKLISTS_SPREADSHEET_ID` (по умолчанию: `17AqmwqceblrAzlHfSztc80cmGJJ71B_godpYoK0QHrc` — «*Tectum - Журнал чек-листов и премирования*»).
  - Данные автоматически распределяются по **4 листам** с индивидуальным цветовым оформлением:
    1. 📑 **«Чек-листы (Премирование)»** (общий реестр, синий заголовок)
    2. 🟢 **«Чек-листы Мастеров»** (мастера цеха ХЦИ, зелёный заголовок)
    3. 🟠 **«Чек-листы Рабочих»** (прием-передача оборудования по участкам, оранжевый заголовок)
    4. 🟣 **«Инспекции ИТР»** (ежедневный аудит и охрана труда, фиолетовый заголовок)
  - Создание и проверка листов выполняется через функцию `get_or_create_sheet()`.
  - **Хронологический порядок в таблице:** Сортировка записей всегда выполняется по возрастанию (`created_at.asc(), id.asc()`), чтобы новые строки добавлялись вниз таблицы.
- **Изолированная сессия БД для BackgroundTasks:**
  - **Строгое правило:** Запрещено передавать объект сессии `db: Session` из `Depends(get_db)` в `BackgroundTasks.add_task()`, так как сессия закрывается до выполнения фонового потока.
  - Фоновая функция-обёртка `sync_checklists_google_bg()` обязана самостоятельно создавать и закрывать сессию: `db = SessionLocal()` с блоком `try ... finally: db.close()`.

### 23.4. UX и отказоустойчивость фронтенда

- **Мгновенный рендеринг (Static Fallbacks):** Карточки шаблонов чек-листов должны отображаться моментально без блокирующего ожидания ответов сети.
- **Параллельная загрузка (`Promise.allSettled`):** Запросы суточного графика, базы сотрудников и шаблонов выполняются параллельно, чтобы сбой или задержка одного запроса не ломали загрузку интерфейса.
- **Модальные уведомления после отправки (UX):** После нажатия «Сохранить» поля формы очищаются, а на экране появляется универсальное модальное окно уведомления `showNotification` с зеленой галочкой и кнопкой «ОК» (в едином стиле с главным порталом, без лишних промежуточных экранов).

## 24. Корпоративная База Знаний (Knowledge Base Hub)

### 24.1. Архитектура и извлечение метаданных внешних облаков (Google Drive / OneDrive / Yandex)
- **Парсинг Google Docs / Sheets:** При автоматическом определении заголовков Google Таблиц категорически запрещено использовать `table.label` из GViz JSON API, так как это имя первой колонки таблицы (например, «Дата»), а не название самого документа. Извлечение должно производиться из `<meta property="og:title">`, `<meta itemprop="name">` или тега `<title>` страниц `/preview` и `/edit`.
- **Синхронизация заголовков:** Эндпоинты `/api/documents/fetch_link_title` и `/api/documents/sync_external_titles` обязаны очищать служебные суффиксы (` - Google Таблицы`, ` - Google Sheets`, ` - OneDrive`, ` - Excel` и др.) и отсекать стандартные страницы авторизации.

### 24.2. Безопасность и UI/UX ввода паролей
- **Маскирование паролей:** Для ввода паролей к защищенным папкам/ссылкам категорически запрещено использовать стандартный браузерный `prompt()`. Всегда использовать Promise-based кастомные модальные окна с полем `type="password"`, поддержкой отображения по кнопке «глазика» и сохранением токена в `sessionStorage`.
- **Изоляция модальных окон в DOM:** Все модальные окна в `docs.html` должны быть независимыми прямыми потомками `<body>` с корректной закрывающей версткой `</div>`, чтобы исключить скрытие модалок внутри других блоков со стилем `display: none`.

### 24.3. Навигация и мобильная адаптивность
- **Сохранение папки при перезагрузке (F5):** ID текущей открытой папки в Базе Знаний всегда синхронизируется с `window.location.hash` и `sessionStorage`, предотвращая сброс в корень при обновлении страницы.
- **Интерактивный брендинг:** Логотип и заголовок «База Знаний» (в шапке на мобильных и в боковом меню) обязаны быть кликабельными для быстрого возврата в корневой каталог (`navigateToRootFolder`).
- **Мобильный лейаут панели действий:** На экранах $\le 680px$ кнопки действий (`+ Создать`, `🔄 Обновить названия`, быстрые ссылки облаков) выстраиваются вертикальной адаптивной сеткой, исключая горизонтальное вылезание за границы экрана.

## 25. Межподразделенческий планнер задач (Tectum Weekly Planner)

### 25.1. Архитектура и структура Google Таблицы
- **ID таблицы планнера:** `1K6Lk0fVfVpfC7gpvg8Hlpj0IgTF9j5woLOWKquyFewc`
- **Модель 3 листов (против простыни данных):**
  1. ⚡ **«План на неделю»** — компактный экран текущих задач на 1 неделю. 10 колонок: `A: ID`, `B: Зона/Узел`, `C: Ввод задачи`, `D: Задачи (RU)`, `E: Задачи (KZ)`, `F: Автор`, `G: Исполнитель`, `H: Срок`, `I: Статус`, `J: Комментарий / Факт`, а также скрытая колонка `K: Google Task ID`.
  2. 🗄️ **«Архив»** — изолированное хранилище завершенных задач, куда они перемещаются по кнопке меню *«Завершить неделю (Архив)»*.
  3. ⚙️ **«Справочники»** — реестр сотрудников, их корпоративных Email для оповещений, зон/подразделений и годовой сетки недель.

### 25.2. Равноправная двусторонняя синхронизация (Google Sheets ↔ Google Tasks)
- **Идентификация:** Каждая задача, попадающая в мобильный Google Tasks, связывается со строкой таблицы через невидимый Google Task ID в колонке `K (11)`.
- **Симметрия действий:**
  - Создание задачи на сотрудника в таблице $\rightarrow$ появление в мобильном Google Tasks.
  - Закрытие задачи на телефоне $\rightarrow$ автоперевод в `🟢 Выполнено` в таблице.
  - Удаление в таблице $\rightarrow$ автоудаление с телефона; удаление на телефоне $\rightarrow$ автоудаление из таблицы.

### 25.3. Триггеры уведомлений и защита от спама
- **Срабатывание строго по смене статуса:** Почтовые оповещения Gmail (`handleSheetEdit`) не отправляются при наборе текста или смене даты. Триггер срабатывает **строго при изменении значения в колонке Статуса (I)**:
  - `В очереди` / `В работе` $\rightarrow$ письмо исполнителю с описанием, зоной, двуязычным текстом и сроком.
  - `Выполнено` / `Проблема` $\rightarrow$ письмо автору задачи с отчетом или описанием проблемы.
- **Бесшовная модель прав:** Начальники подразделений работают с правами «Редактор» без необходимости настраивать Apps Script на своих устройствах. Скрипты выполняются от имени владельца таблицы.

### 25.4. Календарная сетка рабочих недель (Mon-Fri Week Grid)
- **Строгий интервал Пн–Пт:** Все недели в планнере задач обязаны начинаться в понедельник и заканчиваться в пятницу (`ДД.ММ - ДД.ММ`).
- **Переход через границу месяца:** Запрещено привязывать границы недели к 1-му числу месяца (например, `01 - 07`). Если понедельник выпадает на конец месяца (например, 31 августа), неделя отображается как `31.08 - 04.09` (Пн–Пт) и относится к месяцу своего понедельника.
- **Синхронность Front/Back:** Как в API `/api/tasks/weeks` (`generate_calendar_structure_mon_fri`), так и в клиентском фоллбэке JS (`generateClientFallbackWeeks`) генерация недель должна использовать идентичный алгоритм расчёта понедельников.

### 25.5. Единство эндпоинтов и защита от дублирования контроллеров
- **Запрет дублирования роутов:** В `main.py` должен существовать строго один набор эндпоинтов для задач (`/api/tasks`, `/api/tasks/weeks`, `/api/tasks/{task_id}`, `/api/tasks/archive_week`, `/api/tasks/import_from_google_sheets` и т.д.).
- **Полная зачистка при модернизации:** При рефакторинге, создании новых версий или переписывании модуля устаревшие эндпоинты обязаны удаляться полностью, а не дописываться ниже по коду, чтобы FastAPI не перехватывал запросы устаревшими схемами.

### 25.6. Чистота селектора недель и валидация данных
- **Строгая генерация списка недель:** Эндпоинт `/api/tasks/weeks` возвращает строго чистую математическую календарную сетку. Запрещено подмешивать невалидированные текстовые строки `week_label` из базы данных, чтобы исключить появление фантомных или дублирующихся недель («Неделя 1», «Неделя 1 (01 - 07)») в селекторе.
- **Синхронизация экспорта в Google Sheets:** Функция экспорта `export_tasks_to_google_sheets` обязана синхронно поддерживать актуальные поля модели `models.Task` (`code`, `zone`, `title`, `title_kz`, `photo_link`, `author_name`, `assignee_name`, `due_date_str`, `status`, `comment`, `month_label`, `week_label`).

### 25.7. Управление справочниками и Email в Админ-панели (Planner Settings)
- **Сотрудники планнера (`models.PlannerEmployee`):** Авторы и исполнители задач хранятся в таблице `planner_employees` (`name`, `email`, `is_active`, `sort_order`). Управление осуществляется в Панели Администратора во вкладке **«Настройки планнера»** (`#tab-planner-settings`) через эндпоинты `/api/planner/employees` (полный CRUD).
- **Зоны и подразделения (`models.PlannerZone`):** Справочник зон/направлений задач (`name`, `is_active`, `sort_order`) управляется через `/api/planner/zones` (полный CRUD).
- **Запрет хардкода списков:** Списки авторов, исполнителей и зон в интерфейсе планнера (`tasks.html` / `tasks.js`) обязаны динамически загружаться с бэкенда функцией `loadPlannerDropdownData()`.
- **Маршрутизация Email-уведомлений:** Функция `get_task_person_email(db, person_name)` ищет персональный email сначала в `models.PlannerEmployee`, затем при необходимости делает фоллбэк на `models.Master`. Уведомления отправляются:
  - Исполнителю — при создании задачи и при взятии в работу (`🟡 В работе`).
  - Автору — при завершении (`🟢 Выполнено`) или переносе (`🔵 Перенесено`).

### 25.8. Двухколоночный автоперевод задач (RU ⇄ KZ), надежный API и подтверждение выполнения
- **Двухколоночный UX ввода задач (`tasks.html` / `tasks.js`):**
  - Модальное окно содержит две колонки: **«Задача»** (основной ввод на любом языке) и **«Перевод»** (автоматический перевод на лету).
  - Ручная кнопка переключения ликвидирована — интерфейс автоматически распознаёт язык левой колонки и динамически меняет заголовок правой (`Перевод на казахский (KZ)` или `Перевод на русский (RU)`).
  - При сохранении задачи бэкенд и фронтенд гарантированно распределяют русский текст в `title`, а казахский в `title_kz`.
- **Интеллектуальный анализатор и стабильный API (`detect_and_translate_task_text`):**
  - **Запрет нестабильного endpoint `translate_a/single?client=gtx`:** Общедоступный `gtx` endpoint возвращает `429 Too Many Requests` на облачных серверах Railway.
  - **Стандарт API перевода:** Использовать исключительно Google Clients API `https://clients5.google.com/translate_a/t?client=dict-chrome-ex` с браузерным `User-Agent` + резервный фоллбэк на `MyMemory API`.
  - **Определение языка:** Анализ специфических казахских символов (`ә, і, ң, ғ, ү, ұ, қ, ө, һ, Ә, І, Ң, Ғ, Ү, Ұ, Қ, Ө, Һ`) и словаря частых слов (`сәлем`, `жұмыс`, `керек`, `ауыстыру`, `тексеру` и др.) с последующим автоопределением через Google Clients API.
  - В `main.py` модуль `re` обязан импортироваться глобально в заголовке файла.
- **Выбор срока задачи (Календарь):**
  - В модальном окне задачи используется нативный календарь `<input type="date">` без перегрузки лишними быстрыми кнопками.
  - Дата форматируется для таблицы с днём недели (например: `28.08 (Пт)`).
- **Весомое подтверждение выполнения задачи:**
  - При переводе задачи в статус `🟢 Выполнено` (из таблицы или формы) обязательно открывается модальное окно сдачи результата:
    - Поле **«Что сделано / Факт выполнения»** — **обязательно** для заполнения (текст результата).
    - Поле **«Ссылка на фото»** (Google Drive / Railway Volume) — **необязательно**.
    - При отмене диалога статус в таблице мгновенно возвращается к предыдущему без изменений в БД.

### 25.9. Недельные срезы задач без Архива и Режим долгов (Persistent Weeks Architecture)
- **Упразднение ручного «Завершения недели» и «Архива»:** В планнере ликвидирована концепция скрытого листа/режима «Архив» и кнопка «Завершить неделю». Каждая календарная неделя является самостоятельным историческим срезом. Выполненные задачи навсегда остаются в своей неделе с полными фактами и комментариями.
- **Всегда доступная история:** Руководитель или мастер может переключиться на любую неделю любого месяца и увидеть актуальную картину на тот момент.
- **Режим «Долги с прошлых недель» (`include_backlog`):**
  - В панели инструментов доступен переключатель **«Долги с прошлых недель»**.
  - При активации бэкенд (`GET /api/tasks?include_backlog=true`) возвращает как задачи выбранной недели, так и незавершённые задачи (`status != '🟢 Выполнено'`) из других/предыдущих недель.
  - На фронтенде переходящие задачи выделяются наглядным янтарным бейджем `⏳ Неделя X` рядом с кодом задачи.

### 25.10. Светлая тема по умолчанию и Печать/PDF для Инфостенда (Infostand Export)
- **Стандарт темы оформления:** Базовой темой планнера задач является **чистая светлая корпоративная тема** (`#ffffff` / `#f8fafc`) для максимального удобства чтения, контрастности и естественной подготовки документов к распечатке.
- **Печать и PDF для инфостенда:**
  - Кнопки **«Печать»** (`printTasksPlanner`) и **«PDF»** (`exportTasksToPdf`) позволяют в 1 клик распечатать или экспортировать текущую отфильтрованную выборку задач.
  - При печати активируется специальный `@media print` макет (A4 альбомный): отображается официальный заголовок для инфостенда с логотипом Tectum, текущим периодом, перечнем активных фильтров (зона, исполнитель, статус) и точной датой формирования.
  - Все служебные кнопки, селекторы и колонка «Действия» на печати автоматически скрываются.

### 25.11. Интеграция ссылок Google Фото (Zero Database Overhead)
- **Хранение только URL-ссылок:** Фотографии задач и фактов выполнения не загружаются бинарными файлами в базу данных, а хранятся исключительно в виде прямых ссылок (`photo_link`) на Google Фото или Google Диск. Это исключает раздувание БД и сохраняет максимальное быстродействие.
- **Интерактивные помощники выбора:** В формах создания задачи и подтверждения выполнения предусмотрены кнопки:
  - **«Google Фото» (`openGooglePhotosPicker`):** открывает Google Фото в браузере или нативное приложение на смартфоне для быстрого создания ссылки доступа (`Поделиться` ➔ `Создать ссылку`).
  - **«Вставить ссылку» (`pastePhotoLinkFromClipboard`):** в 1 клик считывает и валидирует ссылку из буфера обмена смартфона или ПК.
  - **«Просмотр» (`testOpenPhotoLink`):** позволяет мгновенно проверить прикрепленное изображение.

### 25.12. Запрет удаления задач в обычном интерфейсе (Admin-Only Deletion)
- **Удаление из обычного интерфейса ликвидировано:** В планнере задач (`tasks.html` / `tasks.js`) удалена кнопка корзины («Удалить») из таблицы задач и клиентская функция удаления. Пользователи не могут случайно или намеренно удалить задачи.
- **Удаление исключительно через панель администратора:**
  - Во вкладке админки «Настройки Планнера» (`admin.html`) добавлен «Реестр задач планнера» с живым поиском и кнопкой безвозвратного удаления с подтверждением.
  - Каждое удаление задачи фиксируется в журнале аудита `AuditLog` с сохранением параметров удаленной задачи (ID, код, заголовок, зона, период).

### 25.13. Архитектура почтовых уведомлений (Email Notification Engine)
- **Фоновая асинхронная отправка:** Все email-уведомления (`email_service.py`) отправляются в фоновом потоке FastAPI `BackgroundTasks`, гарантируя мгновенный ответ API при сохранении задач без задержек.
- **Сценарии доставки:**
  - **Назначение задачи:** Исполнитель получает брендированное письмо с деталями задачи (RU/KZ), зоной, сроком и автором.
  - **Выполнение задачи:** Автор задачи получает отчет о сдаче с фактом выполнения и ссылкой на фото.
  - **Перенос срока:** Автор и исполнитель уведомляются о переносе на следующую неделю.
- **Стандарт конфигурации SMTP:** Отправка работает через переменные окружения `SMTP_HOST` (по умолчанию `smtp.gmail.com`), `SMTP_PORT` (587), `SMTP_USER`, `SMTP_PASSWORD` (пароль приложения) и `SMTP_FROM_NAME`.
- **Тестирование почты:** В админке доступна кнопка быстрой проверки доставки тестового письма (`POST /api/planner/test_email`).

### 25.14. Авторизация по PIN-коду и разграничение прав в Планнере
- **Сотрудники и PIN:** В модели `models.PlannerEmployee` хранится 4-значный `pin_code`. Управление PIN-кодами доступно в Админ-панели (`/admin`).
- **Сессия на клиенте:** Авторизованный сотрудник сохраняется в `sessionStorage`. В шапке отображается бейдж `[👤 Имя] [×]`.
- **Проверка прав (Backend & Frontend):**
  - Создавать задачу разрешено только с валидным PIN автора.
  - Переводить в `🟢 Выполнено` может только Исполнитель, Автор или Администратор.
  - Переносить (`🔵 Перенесено`), редактировать или отменять (`🔴 Отменено`) задачу может только Автор или Администратор.
- **Модальные слои (z-index):** Окно ввода PIN-кода (`#planner-pin-modal`) обязано иметь `z-index: 5000`, чтобы перекрывать любые открытые формы на мобильных устройствах.

### 25.15. Неизменяемость завершенных и отмененных задач (Task Immutability)
- **Запрет редактирования:** Для задач в статусах `🟢 Выполнено` и `🔴 Отменено` кнопки редактирования, переноса, инлайн-комментариев и селектор статуса переводятся в неактивное состояние (`disabled`).
- **Статус `🔴 Отменено`:** При отмене автор указывает причину, которая фиксируется в комментарии вида `[Отменено] <причина>`, а текст задачи отображается зачеркнутым (`.task-cancelled-text`).
- **Административный контроль:** Полное редактирование и удаление таких задач доступно исключительно администраторам через панель администратора.

### 25.16. Постоянное хранилище фото (Railway Volume) и сжатие в WebP
- **Клиентская компрессия:** Браузер на клиенте через HTML5 Canvas пережимает любые фотографии со смартфона до `max 1600px` в формат `.webp` с качеством 82% (размер ~150–250 Кб).
- **Постоянный диск Railway Volume:** 
  - Все загруженные фотографии задач сохраняются в директорию `/app/uploads/tasks/`, подключенную к постоянному Railway Volume (`/app/uploads`).
  - В FastAPI подключен статический роут `/uploads` -> `StaticFiles(directory="uploads")`.
  - Это гарантирует 100% сохранность файлов при пересборках (`git push`) и деплоях сервиса.
- **Всплывающий просмотрщик (Lightbox):** Клик на кнопку `[📷 Фото]` открывает снимок во встроенном всплывающем модальном окне (`#photo-viewer-modal`, `z-index: 6000`) с темным полупрозрачным фоном без открытия внешних вкладок.

### 25.17. Архитектура многопользовательской фильтрации и смарт-чипсов в Планнере
- **Панель быстрых пресетов (`.quick-filters-bar`):**
  - Чипсы быстрого доступа: `[ 🔘 Все задачи ]`, `[ 👤 Мои задачи ]`, `[ ⚡ В работе ]`, `[ ОГЭ ]`, `[ ОГМ ]`, `[ СКК ]`, `[ Бережливое пр-во ]`.
  - Мобильная адаптивность: горизонтальный скролл чипсов обязан скрывать серый системный скроллбар (`scrollbar-width: none; ::-webkit-scrollbar { display: none; }`).
- **Персональный фокус («👤 Мои задачи»):**
  - Запрос к API отправляет параметр `my_person=<ФИО>`, который ищет совпадение по `assignee_name` ИЛИ `author_name`.
  - Включение «Моих задач» автоматически сбрасывает одиночные выпадающие фильтры автора/исполнителя во избежание конфликтующих условий.
  - Клик на «🔘 Все задачи» действует как мастер-сброс: отключает флаг `myTasksFilterActive` и возвращает общий вид.
- **Обзор «За всё время» и «Весь месяц»:**
  - Селектор месяцев содержит опцию `<option value="all">🌐 За всё время</option>`.
  - Селектор недель содержит опцию `<option value="all">📅 Весь месяц (все недели)</option>`.
  - Бэкенд `GET /api/tasks` при `month == 'all'` или `week == 'all'` не накладывает ограничений по соответствующему полю, возвращая задачи за весь период.
- **Запрет дублирования функций в JS:**
  - В клиентских модулях (`tasks.js`, `main.js`) запрещено оставлять дублирующие объявления функций загрузки и синхронизации. Каждая функция жизненного цикла данных (`loadTasks`, `renderTasksTable`) должна быть строго в единственном экземпляре.
- **Обязательное версионирование кэша скриптов:**
  - При любых правках JS/CSS на проде в `tasks.html` инкрементировать версию скрипта (`?v=YYYYMMDD_...`).

### 25.18. Стандарты мобильного интерфейса и жизненного цикла статусов (Mobile UX & In-Work Default)
- **Исключение статуса «В очереди»:**
  - В планнере ликвидирован промежуточный статус «⚪ В очереди».
  - Любая новая задача создаётся строго со статусом по умолчанию **«🟡 В работе»**.
  - Допустимый набор статусов: `🟡 В работе`, `🟢 Выполнено`, `🔵 Перенесено`, `🔴 Отменено`.
- **2-строчный компактный мобильный тулбар ($\le 768\text{px}$):**
  - **1-я строка (`.toolbar-group-dates`):** Селекторы месяца (`#filter-month`) и недели (`#filter-week`) располагаются строго в один ряд (сетка 50/50).
  - **2-я строка (`.toolbar-group-actions`):** Кнопки `[ ⏳ Долги ]`, `[ 🔍 Фильтры ]` и `[ ➕ Задача ]` выстраиваются в один горизонтальный ряд на всю ширину.
  - Категорически запрещено растягивать выпадающие списки и кнопки на 4 отдельные вертикальные строки на смартфонах.
- **Эргономика карточек задач (`.planner-card`):**
  - **Полноразмерные сплит-кнопки (50/50):** Кнопки «➡️ Перенести» и «✏️ Редактировать» обязаны занимать 100% ширины подвала карточки (`flex: 1`, высота $\ge 40\text{px}$) для удобного нажатия большим пальцем. Кнопка «Редактировать» выделяется контрастным синим стилем (`#eff6ff` / `#1d4ed8`).
  - **Touch-target статуса:** Высота селектора статуса на карточке обязана быть не менее $38\text{px}$ с крупным жирным шрифтом (`font-weight: 700`) и контрастными цветными бейджами.
  - **Интерактивная плашка добавления факта:** Кнопка добавления комментария/факта оформляется в виде заметной пунктирной плашки (`border: 1px dashed #cbd5e1; background: #f8fafc;`) с удобной областью нажатия.
  - **Кнопка фото:** Если фото прикреплено — отображается яркая голубая кнопка `[📷 Фото]`; если фото отсутствует — текстовый плейсхолдер «Без фото» скрывается, освобождая пространство.











